{
  "name": "@acro/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "acro": "./src/cli.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "e2e": "node scripts/e2e-cli.ts",
    "build": "esbuild src/cli.ts --bundle --platform=node --format=cjs --outfile=dist/cli.cjs --log-level=warning"
  },
  "dependencies": {
    "@acro/protocol": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3"
  }
}
